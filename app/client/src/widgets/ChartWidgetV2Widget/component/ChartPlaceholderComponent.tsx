import type { WidgetPositionProps } from "widgets/BaseWidget";
import React from 'react'

export default function ChartPlaceholderComponent (props : WidgetPositionProps) {
    return(
        <div>
            This is placeholder component
        </div>
    )
}